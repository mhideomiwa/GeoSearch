'use client';
import React from 'react';
import Map from '../components/map';

const Page = () => {
    return (
        <div>
            Two Player
            <Map/>
        </div>
    );
};

export default Page;